The evidence specifies stratified sampling, also described as balanced strata.
The calibration set has exactly 64 observations and uses a learning rate of 0.2.
These design choices follow \cite{doe2024}; no comparative superiority claim was tested.
